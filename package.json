{
  "name": "the-face-of-it",
  "version": "5.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test:logic": "node --experimental-strip-types tests/logic.test.ts && node --experimental-strip-types tests/flow.test.ts && node --experimental-strip-types tests/regression.test.ts && node --experimental-strip-types tests/cinematic.test.ts && node --experimental-strip-types tests/scene-flow.test.ts && node --experimental-strip-types tests/design-regression.test.ts && node --experimental-strip-types tests/dialogue-media.test.ts && node --experimental-strip-types tests/release-regression.test.ts",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview",
    "verify": "npm run sync:art && npm run audit && npm run typecheck && npm run test:logic && npm run build && npm run audit:dist",
    "audit": "node scripts/audit-project.mjs",
    "sync:art": "node scripts/sync-original-art.mjs",
    "predev": "npm run sync:art",
    "prebuild": "npm run sync:art",
    "audit:dist": "node scripts/audit-dist.mjs"
  },
  "dependencies": {
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "typescript": "5.7.2",
    "vite": "6.3.1"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "packageManager": "npm@10.9.2"
}
